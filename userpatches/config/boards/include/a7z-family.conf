# Shared userpatch for Radxa Cubie A7Z boards (radxa-cubie-a7z, radxa-cubie-a7z-ufs).
# Sourced after each board's main config under config/boards/.
#
# The sun60iw2 family already sets INSTALL_HEADERS=yes; we only need
# build-time and runtime packages for wfb-ng (svpcom rtl8812au DKMS build).
PACKAGE_LIST_BOARD="arping python3-venv vim iw rfkill build-essential dkms"
